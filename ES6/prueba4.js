function raiz(pNumero) {
    var resultado = Math.sqrt(pNumero);
    return resultado;
}

var resultado = raiz(16);
console.log(`La raiz cuadrada del numero ${resultado * resultado} es ${resultado}`);
